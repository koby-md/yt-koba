import { CustomResponse, HttpClientConfig, HttpResponse } from "./Types";
declare function HttpGet<T>(endpoint: string, url: string, version: string, timeout: number, baseUrl: string): Promise<T>;
declare class HttpError extends Error {
    status: number;
    response?: HttpResponse<unknown> | undefined;
    constructor(status: number, message: string, response?: HttpResponse<unknown> | undefined);
}
declare class HttpClient {
    private config;
    constructor(config: HttpClientConfig);
    get<T>(endpoint: string, queryParams?: Record<string, string>, customHeaders?: Record<string, string>): Promise<HttpResponse<T>>;
    post<T, D = unknown>(endpoint: string, data: D, customHeaders?: Record<string, string>): Promise<HttpResponse<T>>;
    private handleError;
}
export { HttpGet };
export type { CustomResponse, HttpClientConfig, HttpResponse, HttpError };
export { HttpClient };
//# sourceMappingURL=HttpClient.d.ts.map